using UnityEngine;

public class Audio : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    [Header("Weapon")]
    public AudioClip LR7Shoot;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
